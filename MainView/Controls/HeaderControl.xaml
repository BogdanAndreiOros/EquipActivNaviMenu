<UserControl x:Class="MainView.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainView.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="{StaticResource HeaderBackgroundBrush}">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="600*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left" >
            <Image Margin="0 0 20 0" Source="/Images/HeaderImage.png" Width="90" />
            <TextBlock FontSize="25" Foreground="{StaticResource HeaderForegroundBrush}" VerticalAlignment="Center" 
                       Text="Equipment and Activities Navigation" FontWeight="Heavy"/>
        </StackPanel>
        
    </Grid>
    
</UserControl>
