<UserControl x:Class="MainView.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainView.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{StaticResource CategoryViewBackgroundBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Add new entry" 
                Height="20" Width="90" HorizontalAlignment="Right" Margin="0 0 10 0"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding CategoryItems}"/>

    </Grid>
    
</UserControl>
